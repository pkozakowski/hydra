let hours = ../../Duration/hours

let hourly
      : ./MetricCalculator
     -> ./Metric
      = \(calculator : ./MetricCalculator)
     -> { calculator
        , period = hours 1
        }

in hourly
