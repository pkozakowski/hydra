let seconds = ../../Duration/seconds

let secondly
      : ./MetricCalculator
     -> ./Metric
      = \(calculator : ./MetricCalculator)
     -> { calculator
        , period = seconds 1
        }

in secondly
