let minutes = ../../Duration/minutes

let minutely
      : ./MetricCalculator
     -> ./Metric
      = \(calculator : ./MetricCalculator)
     -> { calculator
        , period = minutes 1
        }

in minutely
