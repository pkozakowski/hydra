let NonEmpty = https://prelude.dhall-lang.org/NonEmpty/Type.dhall

let Choose
      : Type -> Type -> Type
      = \(Grid : Type)
     -> \(a : Type)
     -> { xs : NonEmpty a
        , cont : a -> Grid
        }

in Choose
