let Strategy = ../Strategy/Type

let Grid = ./Grid
let GridBuilder = ./GridBuilder

let pure
      : Strategy -> Grid
      = \(Strategy : Strategy)
     -> \(Grid : Type)
     -> \(gridBuilder : GridBuilder Grid)
     -> gridBuilder.pure Strategy

in pure
