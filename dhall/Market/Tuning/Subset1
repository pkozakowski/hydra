let NonEmpty = https://prelude.dhall-lang.org/NonEmpty/Type.dhall

let Subset1
      : Type -> Type -> Type
      = \(Grid : Type)
     -> \(a : Type)
     -> { p : Double
        , xs : NonEmpty a
        , cont : NonEmpty a -> Grid
        }

in Subset1
