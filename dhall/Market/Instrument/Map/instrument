let Entry = https://prelude.dhall-lang.org/Map/Entry

let entry = ../../../Map/entry

let InstrumentName = ../Name
let Instrument = ../Type

in  \(name : InstrumentName)
 -> \(instr : Instrument)
 -> entry InstrumentName Instrument name instr
