let Asset = ../../Asset
let Instrument = ../Type
let InstrumentBuilder = ../Builder

let hold
      : Asset -> Instrument
      = \(asset : Asset)
     -> \(Instrument : Type)
     -> \(instrumentBuilder : InstrumentBuilder Instrument)
     -> instrumentBuilder.hold { held = asset }

in hold
