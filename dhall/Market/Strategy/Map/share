let Entry = https://prelude.dhall-lang.org/Map/Entry

let entry = ../../../Map/entry

let Share = ../../Share
let shareVal = ../../share

let StrategyName = ../Name

in  \(name : StrategyName)
 -> \(value : Double)
 -> entry StrategyName Share name (shareVal value)
